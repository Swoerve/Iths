//let count = prompt("How many alerts do you want?");
// let number = 0;

// while (number < count && count) {
    // alert(number);
    // number++;
// }

let count

do {
    count = prompt()
} while (!count || isNaN(count))


console.log(count)
  
for (let number = 0; number < count && count; number++) {
    alert(number)
}
